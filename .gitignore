# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
build/
dist/

# Playwright browser cache (installed in CI separately)
ms-playwright/

# OS
.DS_Store

# NOTE: data/output/ is intentionally NOT ignored — it is the committed dataset.
